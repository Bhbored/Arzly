namespace Arzly.Shared.DTOs.Request.UserActivityLog
{
    // UserActivityLog is immutable - updates are not supported
    // This DTO exists only to satisfy the BaseService generic type constraints
    public class UserActivityLogUpdateRequest
    {
        // Empty - updates are not allowed for activity logs
    }
}
